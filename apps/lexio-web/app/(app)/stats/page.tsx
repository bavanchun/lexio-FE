/**
 * /stats — placeholder until phase-09 implements statistics view.
 */
export const metadata = { title: 'Stats — Lexio' };

export default function StatsPage() {
  return (
    <div className="flex flex-col gap-4">
      <h1 className="text-2xl font-semibold">Stats</h1>
      <p className="text-muted-foreground">Statistics dashboard coming in phase 09.</p>
    </div>
  );
}
