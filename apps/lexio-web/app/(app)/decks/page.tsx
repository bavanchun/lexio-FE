/**
 * /decks — placeholder until phase-07 implements deck management.
 */
export const metadata = { title: 'Decks — Lexio' };

export default function DecksPage() {
  return (
    <div className="flex flex-col gap-4">
      <h1 className="text-2xl font-semibold">Decks</h1>
      <p className="text-muted-foreground">Deck management coming in phase 07.</p>
    </div>
  );
}
