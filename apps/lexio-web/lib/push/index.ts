/**
 * lib/push — Web Push notification adapter placeholder.
 * Phase-11+ adds service worker registration and push subscription logic.
 *
 * lib/ layer may only import from core/ — never from features/ or app/.
 */
export {};
