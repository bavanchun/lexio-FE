/**
 * lib/api — mock API gateway placeholder.
 * Phase-05 adds the in-memory mock adapter implementing core ports.
 * Phase-07+ replaces with real HTTP client (fetch + Zod validation).
 *
 * This file is the single import point for all API adapters.
 * lib/ layer may only import from core/ — never from features/ or app/.
 */
export {};
