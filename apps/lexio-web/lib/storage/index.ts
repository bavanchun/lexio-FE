/**
 * lib/storage — Dexie (IndexedDB) repository implementations placeholder.
 * Phase-05 adds concrete classes implementing ICardRepository, IDeckRepository,
 * IUserCardRepository, IReviewRepository, ISessionRepository, IStreakRepository,
 * IUserXpRepository, IAchievementRepository from core/ports.
 *
 * lib/ layer may only import from core/ — never from features/ or app/.
 */
export {};
